# 此文件用来控制复制键盘文件的行为
# #符号为注释
# 其余内容每一行为一个正则表达式，只有匹配正则的键盘文件才会复制到应用目录

# 不复制通用 txt，避免键盘旧规则文件覆盖应用目录规则文件。
# 不复制通用 tsv，避免键盘旧 zzc_reset.tsv 覆盖 iCloud 下发的新 reset marker。

# zzc_state 运行状态从键盘回传到应用目录，只回传待合并运行态。
(^|.*[\\/])zzc_state[\\/]runtime_ops[.]tsv$
(^|.*[\\/])zzc_state[\\/]runtime_exact[.]tsv$
(^|.*[\\/])zzc_state[\\/]effective_state[.]tsv$
(^|.*[\\/])zzc_state[\\/]zzc_reset_seen[.]tsv$

# 持久 zzc 操作文件从键盘回传到应用目录。
(^|.*[\\/]).*[.]zzc[.]dict[.]yaml$